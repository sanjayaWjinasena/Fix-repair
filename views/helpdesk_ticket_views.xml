<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="helpdesk_ticket_form_assign_to_me" model="ir.ui.view">
        <field name="name">helpdesk.ticket.form.assign.to.me</field>
        <field name="model">helpdesk.ticket</field>
        <field name="inherit_id" ref="helpdesk.helpdesk_ticket_view_form"/>
        <field name="arch" type="xml">
            <!-- Stored computed field — web_read reads a plain DB varchar, no many2one traversal -->
            <xpath expr="//field[@name='name']" position="before">
                <field name="repair_stage_state" invisible="1"/>
                <field name="task_done" invisible="1"/>
                <field name="repair_picking_count" invisible="1"/>
                <field name="has_ready_dispatch_picking" invisible="1"/>
            </xpath>

            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_view_repair_pickings"
                        type="object"
                        class="oe_stat_button"
                        icon="fa-truck"
                        invisible="repair_picking_count == 0">
                    <field name="repair_picking_count" widget="statinfo" string="Movements"/>
                </button>
            </xpath>

            <!-- Hide the standard helpdesk_stock 'Returns' button — it
                 shows only the ticket's standard picking_ids subset, while
                 our Movements button above captures every transfer
                 stamped with x_studio_helpdesk_ticket_id (returns,
                 dispatches, factory transit, etc). Kept unconditional
                 because our Movements button replaces it on every ticket
                 that renders this form. -->
            <xpath expr="//button[@name='action_view_pickings']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>

            <xpath expr="//header" position="inside">

                <button name="action_send_to_factory"
                        type="object"
                        string="Send to Factory"
                        class="btn-primary"
                        invisible="repair_stage_state != 'new'"/>

                <button name="action_received_at_factory"
                        type="object"
                        string="Received at Factory"
                        class="btn-primary"
                        invisible="repair_stage_state != 'sent_to_factory'"/>

                <button name="action_send_to_sales_centre"
                        type="object"
                        string="Send to Sales Centre"
                        class="btn-primary"
                        invisible="not task_done"/>

                <button name="action_received_at_sales_centre"
                        type="object"
                        string="Received at Sales Centre"
                        class="btn-primary"
                        invisible="repair_stage_state != 'sent_to_sales_centre'"/>

                <button name="action_assign_to_me"
                        type="object"
                        string="Assign to Me"
                        class="btn-secondary"
                        invisible="user_id == uid"/>

            </xpath>
        </field>
    </record>

    <!--
        Hide a set of ticket-form fields that no longer belong in the
        current UX. All of these keep their DB columns, computes, and
        constraints — they're just no longer rendered on the form.

        Priority 99 to force Odoo to apply this view AFTER Studio's
        own inherits — otherwise the xpath expressions could resolve
        before Studio has finished inserting these fields into the
        merged arch, and the position="attributes" op would be a no-op.
    -->
    <record id="helpdesk_ticket_form_hide_fields" model="ir.ui.view">
        <field name="name">helpdesk.ticket.form.hide.fields</field>
        <field name="model">helpdesk.ticket</field>
        <field name="inherit_id" ref="helpdesk.helpdesk_ticket_view_form"/>
        <field name="priority">99</field>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='team_id']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
            <xpath expr="//field[@name='x_studio_rug_repair']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
            <xpath expr="//field[@name='x_studio_rug_confirmed']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
            <xpath expr="//field[@name='x_studio_quick_repair_status']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
            <xpath expr="//field[@name='x_studio_cancel_reason']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
            <xpath expr="//field[@name='x_studio_items']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
            <xpath expr="//field[@name='x_studio_source_location_1']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
            <xpath expr="//field[@name='x_studio_tracking']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
            <xpath expr="//field[@name='x_studio_normal_repair_with_serial_no']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
            <xpath expr="//field[@name='x_studio_normal_repair_without_serial_no']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
        </field>
    </record>

</odoo>
